# Archive

This folder contains code that is no longer part of the active build.

## frontend-next-wrapper

The archived `frontend-next-wrapper` project is the old Next.js/Capacitor web
wrapper. It is kept for reference only. The active app is:

```text
../servicios-elpaisano-expo
```
